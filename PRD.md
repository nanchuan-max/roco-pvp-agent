# PRD：洛克王国世界 PVP 全自动对战 Agent

> 本文档基于 grill-me 设计会话生成，涵盖 rocom-pvp-agent 的完整产品需求与技术架构。

---

## Problem Statement

用户在玩《洛克王国世界》手游的 PVP 天梯对战时，面临以下核心痛点：

1. **决策复杂度极高**：每回合需在 1 分钟内，从 4 个技能、聚能、切换精灵、逃跑等动作中选出最优解，同时考虑属性克制、伤害预估、能量管理、印记覆盖、敌方隐藏技能池等多维因素。
2. **精确计算困难**：伤害公式、种族值实际面板、印记叠加效果等数值计算复杂，人类玩家难以在实时对战中完成精确心算。
3. **隐藏信息推理负担重**：敌方 6 只精灵的技能组合和血脉类型初始完全隐藏，需根据敌方每回合的暴露行为逐步更新信念状态（Belief State），这对人类的短期记忆和概率推理能力要求很高。
4. **回合节奏压力大**：PVP 每回合限时 60 秒，人类玩家容易因时间压力做出次优决策。

因此，用户需要一个**全自动 AI Agent**，能够接管对战的完整生命周期——从队伍识别、首发选择、回合状态感知、局势分析到键盘操作执行——让玩家在 PVP 中获得稳定且高效的自动化对战能力。

---

## Solution

构建 **rocom-pvp-agent**：一个基于 **LangGraph** 框架的 PVP 全自动对战 Agent。

系统核心流程：

1. **外层轮询**：一个高频 Python 循环（500ms）持续截图并检测游戏 UI 状态（队伍展示 / 选首发 / 我的回合 / 等待 / 对战结束）。
2. **对战初始化（InitGraph）**：检测到队伍展示界面后，视觉识别双方 12 只精灵名称，从知识库预加载完整数据；LLM 分析双方阵容后决策首发精灵，通过键盘指令完成选择。
3. **回合决策（BattleGraph）**：检测到我方回合时，启动 LangGraph：
   - **perceive**：截图通过视觉模型解析为结构化 JSON（当前 HP、能量、精灵名、存活指示器等）。
   - **update_state**：将感知结果与持久化记忆合并，更新 BattleState（包括敌方信念状态、印记、魔力等）。
   - **decide**：LLM 在系统提示中接收当前状态 + 最近 5 回合历史 + 可用 Tools，输出结构化决策 JSON。
   - **execute**：通过键盘模拟发送指令（1/2/3/4 选技能、E 切换、R 聚能、Esc 逃跑等）。
4. **记忆系统**：每回合状态以 JSONL 格式追加到外部文件，支持复盘和 LLM 上下文加载。

Agent 运行环境为官方 PC 客户端，输入方式为键盘快捷键，视觉来源为屏幕截图。

---

## User Stories

1. 作为玩家，我希望 Agent 能在对战开始前自动识别双方队伍展示界面中的 12 只精灵，以便无需手动输入阵容信息。
2. 作为玩家，我希望 Agent 能根据双方阵容的克制关系、速度线和种族值，自动决策首发精灵，以便抢占开局优势。
3. 作为玩家，我希望 Agent 在我方回合开始时自动截取对战画面，并解析为结构化的状态数据，以便后续决策。
4. 作为玩家，我希望 Agent 能精确计算任意技能对任意精灵的预估伤害（考虑克制、印记、增益减益），以便判断能否击杀或需否防御。
5. 作为玩家，我希望 Agent 能根据种族值、个体值（固定 60 分配 3 项）和性格，计算精灵的实际面板数值，以便进行速度线比较和伤害预估。
6. 作为玩家，我希望 Agent 能通过 Tools 按需查询知识库中的精灵、技能和规则细节，以便在需要时补全决策上下文。
7. 作为玩家，我希望 Agent 在每回合基于当前局势自动选择最优技能（1-4 号位），并说明推理过程，以便我理解决策逻辑。
8. 作为玩家，我希望 Agent 能在不利局势下（如被克制、残血、能量不足）自动决策切换精灵，并通过键盘执行换人操作。
9. 作为玩家，我希望 Agent 能在能量不足且局势允许时自动选择聚能（恢复能量），以便为下回合高能耗技能做准备。
10. 作为玩家，我希望 Agent 能实时跟踪并更新敌方每只精灵的已暴露技能列表，形成信念状态，以便预测对手后续行动。
11. 作为玩家，我希望 Agent 能记录敌方精灵的血脉推断结果（基于特性触发行为），以便调整属性克制预期。
12. 作为玩家，我希望 Agent 能完整记录每一回合双方的行动（技能/切换/聚能/逃跑），以便赛后复盘和对战分析。
13. 作为玩家，我希望 Agent 能正确跟踪魔力制（初始 4 点，击败扣 1 点，特性可修改），以便在残局时调整策略。
14. 作为玩家，我希望 Agent 能正确跟踪能量系统（每只精灵 10 点上限，切换保留，部分技能回复），以便管理技能释放节奏。
15. 作为玩家，我希望 Agent 能正确跟踪印记系统（正面 8 种 / 负面 8 种，同阵营各限 1 种，新印记覆盖旧印记），以便利用印记优势。
16. 作为玩家，我希望 Agent 能处理精灵切换下场后状态保留的机制（HP 和能量不变），并在其再次上场时恢复可见状态。
17. 作为玩家，我希望 Agent 支持对战结束检测，并在对战结束后自动归档对战日志，以便长期统计胜率。
18. 作为玩家，我希望 Agent 的外层轮询不会占用过高 CPU，且能在 1 分钟的回合限制内完成决策，以便不影响游戏体验。
19. 作为玩家，我希望 Agent 的知识库预加载机制能一次性加载本场对战涉及的 12 只精灵完整数据，而不是每回合反复查文件，以便降低延迟。
20. 作为玩家，我希望 Agent 的通用 PVP 规则（魔力制、速度判定、印记规则、属性克制）作为系统提示常驻，而不是每回合重新加载，以便提升决策一致性。

---

## Implementation Decisions

### 1. 外层架构：轮询循环 + LangGraph 分离

采用外层 Python `while` 循环负责高频 UI 检测（500ms 轮询），LangGraph 仅负责一次性决策流程（InitGraph 和 BattleGraph）。这样避免在 Graph 内部实现阻塞式等待，保持 LangGraph 的纯粹性。

外层状态机：
- `unknown` → 持续轮询
- `team_select` → 触发 InitGraph
- `first_pet_select` → InitGraph 内部处理或等待 InitGraph 完成
- `battle_my_turn` → 触发 BattleGraph
- `battle_wait` → 空转等待
- `battle_end` → 归档日志，重置状态

### 2. 双 Graph 设计：InitGraph vs BattleGraph

- **InitGraph**：负责对战初始化。节点包括：`team_perceive`（识别 12 只精灵）→ `decide_first_pet`（LLM 选首发）→ `execute_select`（键盘执行选择）。
- **BattleGraph**：负责单回合决策。节点包括：`perceive`（视觉解析）→ `update_state`（状态合并）→ `decide`（LLM 决策）→ `execute`（键盘执行）。

两个 Graph 独立编译，外层根据 UI 状态决定调用哪个。

### 3. 核心状态模型（Pydantic）

使用 Pydantic 模型保证类型安全和序列化能力。核心模型包括：

- `BattleState`：整场对战的全局状态（turn_number、双方魔力、双方 6 只精灵、历史记录）。
- `PetState`：单只精灵的状态（名称、属性、HP/能量、4 个技能、印记、状态效果、血脉等）。
- `Skill`：技能（名称、槽位、能耗、威力、类型、属性、效果描述、known 标记）。
- `TurnLog`：单回合记录（行动双方、时间戳、备注）。
- `TurnAction`：具体行动（技能/切换/聚能/逃跑 + 详情）。

关键设计：
- 我方精灵的 `skills` 初始完整且 `known=True`。
- 敌方精灵的 `skills` 初始为空列表且 `known=False`，随对战逐步填充。
- `opponent_exposed_skills` 字典记录敌方每只精灵已暴露的技能名列表。

### 4. 知识库加载策略：预加载 + Tool 按需查询

- **预加载**：InitGraph 识别 12 只精灵后，立即从知识库目录加载这 12 只精灵的完整数据（种族值、特性、技能池、首领化信息）到内存字典。PVP 通用规则（魔力制、印记规则、速度判定、克制表）作为系统提示常量直接嵌入 Prompt，不通过 Tool 查询。
- **按需查询**：仅当 LLM 需要查询某技能的详细效果、某规则的例外情况、或知识库中未预加载的关联信息时，才调用 `query_kb` Tool。

### 5. 视觉解析输出：结构化 JSON

`perceive` 节点的视觉解析必须输出结构化 JSON，而非自由文本。格式示例：

```json
{
  "ui_phase": "battle_my_turn",
  "my_active": {
    "name": "迪莫",
    "hp_current": 80,
    "hp_max": 100,
    "energy": 6,
    "status_effects": [],
    "marks": {"攻击印记": 2}
  },
  "opponent_active": {
    "name": "火花",
    "hp_current": 45,
    "hp_max": 100,
    "energy_visible": false,
    "status_effects": ["灼烧"],
    "marks": {}
  },
  "my_bench_alive_count": 5,
  "opponent_bench_alive_count": 5,
  "field_notes": "敌方上回合使用了技能'闪燃'"
}
```

`update_state` 节点负责将 JSON 数值精确合并到 `BattleState` 中（如 HP 从 100→80，能量从 10→6）。

### 6. Memory 机制：外部 JSONL + 最近 5 回合上下文

- **持久化**：每回合 `execute` 结束后，将完整的 `BattleState` 以 JSONL 格式追加到文件（`battle_logs/{battle_id}.jsonl`）。
- **LLM 上下文**：`decide` 节点的 Prompt 中，只加载最近 5 回合的完整 `TurnLog`，更早的历史由代码层压缩为高层摘要（如"对手偏好火系技能、有换宠习惯"）。
- **信念状态**：敌方隐藏信息（技能和血脉）的更新直接在 `BattleState` 的 `opponent_exposed_skills` 和 `PetState.bloodline` 中维护，随 JSONL 一起持久化。

### 7. Tools 设计

LLM 可调用的 Tools 包括：

- `query_kb(query: str, entity_type: Literal["pet", "skill", "rule"]) -> str`：按需查询知识库。
- `calculate_damage(attacker: PetState, defender: PetState, skill_name: str, field_marks: dict) -> dict`：根据知识库公式计算预估伤害，返回 `{estimated_damage, can_kill, damage_ratio}`。
- `calculate_actual_stats(pet: PetState, individual_values: dict, nature: str) -> dict`：根据种族值 + 个体值 + 性格计算实际面板。

这些 Tools 是纯代码函数，延迟极低（毫秒级），不依赖 LLM。

### 8. 键盘执行层

通过 `pynput` 或 `pydirectinput` 模拟键盘输入。指令映射：

- 技能 1/2/3/4 → 按键 `1` / `2` / `3` / `4`
- 切换精灵 → 先按 `E`，再按方向键/数字键选择目标
- 聚能 → 按 `R`（或对应快捷键）
- 逃跑 → 按 `Esc`

执行层只接收结构化的决策 JSON，不负责任何策略判断。

### 9. 伤害与属性计算公式

严格遵循知识库中的公式：

- HP = round([1.7 × 种族值 + 个体值 × 0.85 + 70] × (1 + 性格修正) + 100)
- 其他属性 = round([1.1 × 种族值 + 个体值 × 0.55 + 10] × (1 + 性格修正) + 50)
- 伤害 = (攻击方对应攻击 × 技能威力/100) × 克制系数 × 防御方对应防御修正 × 其他修正（印记、增益、特性）

个体值固定为 60，可任意分配给 3 项属性；未分配项为 0。性格修正为 +20% / -10% 两项。

### 10. 敌方同名精灵区分

根据对战规则，一方携带的 6 只精灵不会出现重复。因此敌方精灵直接用名称唯一标识，无需额外编号。

### 11. 战报面板与行动推断

战报面板需点击进入查看历史行动。视觉解析在 `perceive` 阶段若无法读取战报（当前回合 UI 不展示），则依赖 `update_state` 的数值 diff 反推行动（如敌方 HP 没掉但我方 HP 掉了 → 敌方使用了攻击技能；敌方能量变化可辅助推断技能能耗）。若战报可读，优先采用战报信息更新 `TurnLog`。

---

## Testing Decisions

### 什么是好的测试

只测试模块的**外部行为与接口契约**，不测试内部实现细节。LLM 决策节点本身具有非确定性，因此不对其"选哪个技能"做单元测试，只测试其输入格式和输出 JSON Schema 的合法性。

### 需要测试的模块

1. **伤害计算器（damage_calc）**
   - 覆盖物理/魔法伤害、克制/抵抗系数、单克制/双克制、印记加成、连击递减。
   - 使用知识库中的已知数值构造测试用例（如迪莫 vs 火花的特定技能）。

2. **属性值计算器（stat_calc）**
   - 覆盖不同种族值、不同个体值分配、不同性格的组合。
   - 验证与知识库文档中的计算示例一致（如迪莫固执性格的实际面板）。

3. **状态更新逻辑（update_state）**
   - 测试 `merge_perception` 函数：给定上一回合的 `BattleState` 和当前 `perceived_json`，验证合并后的数值正确（HP、能量、印记层数）。
   - 测试换下场精灵状态保留：精灵 HP 45、能量 3 下场，再次上场时仍为 45 和 3。

4. **信念状态更新（belief_state）**
   - 测试敌方使用技能后，该技能是否正确加入 `opponent_exposed_skills`。
   - 测试敌方切换精灵后，新旧精灵的 `on_field` 标记是否正确切换。

5. **知识库加载器（kb_loader）**
   - 测试从知识库目录加载指定精灵的完整数据，验证字段完整性。
   - 测试加载不存在的精灵时返回合理错误。

6. **键盘执行器（keyboard）**
   - 使用 mock/stub 测试：验证给定决策 JSON 后，执行层发出的按键序列是否正确。
   - 不在自动化测试中实际发送键盘事件，避免副作用。

### 不测试的内容

- LLM 决策节点的策略质量（非确定性，需通过实际对战评估）。
- 视觉解析节点的准确率（依赖外部视觉模型，需人工标注数据集评估）。
- UI 状态检测的可靠性（依赖游戏 UI 稳定性，需集成测试）。

---

## Out of Scope

1. **强化学习（RL）训练**：本版本基于规则 + LLM 推理，不涉及 MCTS rollout 采样或神经网络策略训练。
2. **内存读取 / 网络抓包**：仅通过视觉截图获取状态，不进行游戏客户端逆向或通信拦截。
3. **自动匹配与排位系统**：Agent 只接管已进入的对战流程，不负责点击匹配按钮、选择模式等战前 UI。
4. **多账号并发**：单实例、单对战。
5. **移动端支持**：仅支持官方 PC 客户端。
6. **全自动 7×24 小时挂机**：需人工启动，单局结束后停止或等待下局。
7. **实时伤害数字的视觉校验**：依赖公式计算，不通过 OCR 读取战斗飘字。
8. **敌方性格与个体值推测**：假设敌方使用标准配置（或用户手动配置敌方性格），不主动推断敌方性格。

---

## Further Notes

1. **延迟优化路径**：当前版本以功能完整性优先，暂不考虑 60 秒时间限制内的极致延迟优化。后续可通过以下方式缩短决策时间：
   - Prompt caching / 预填充系统提示
   - Tool 调用并行化
   - 视觉解析异步化（在对方回合提前截图预解析）
   - 简化 LLM 推理深度（如固定使用 CoT 但限制 token）

2. **伤害公式准确性**：伤害计算器必须严格与知识库文档 `战斗机制详解.md` 中的公式保持一致。任何公式更新都需同步更新测试用例。

3. **视觉解析瓶颈**：`perceive` 节点是整个系统准确率的关键瓶颈。初期应允许用户在关键回合人工确认/纠正视觉解析结果。建议维护一个"视觉解析错误日志"，用于后续迭代改进 prompt 或切换到更精确的视觉模型。

4. **知识库联动**：本 Agent 与现有的 `rocom-pvp-knowledge-base` 知识库共用数据源。精灵和技能数据变更时，Agent 无需修改代码，只需重新加载知识库即可。

5. **对战日志价值**：JSONL 格式的对战日志不仅用于复盘，还可作为后续 RL 训练或策略优化的数据资产。

6. **后续扩展**：当基础框架稳定后，可考虑引入 MCTS 作为 LLM 的辅助 Tool，让 LLM 负责高层策略，MCTS 负责短深度（2-3 回合）的精确搜索。
